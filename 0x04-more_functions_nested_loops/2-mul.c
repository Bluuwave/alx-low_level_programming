#include "main.h"

/**
 * mul - multiplies two integers
 * 0x: the first number to multiply
 * 0y: the second number to multiply with first number
 * Return: the result of multiplication between x and y
 */
int mul(int x, int y)
{

	return (x * y);
}
